import "./header.css";

const CTA = () => {
  return (
    <div className="cta">
        <a href="#contact" className="btn btn-primary">Kontakt</a>
        <a href="/shop" className="btn btn-primary">Shop</a>
    </div>
  );
};

export default CTA;
